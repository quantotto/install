apiVersion: v1
kind: Service
metadata:
  name: {{ include "quantotto.fullname" . }}
  labels:
    {{- include "quantotto.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quantotto.selectorLabels" . | nindent 4 }}
